[package]
name = "chua-native-heif"
version = "0.1.0"
edition = "2021"

[lib]
name = "chua_native_heif"
crate-type = ["cdylib"]

[dependencies]
# 静态编译 libheif + libx265，不依赖系统 DLL
libheif-src = "1.23"

# 日志（可选，编译时关闭可去掉）
log = "0.4"

[profile.release]
opt-level = 3
lto = true
strip = true
