# 爬虫中心重构 — 实施计划

> 重构目标：基于现有 `utils-support-flow-starter` + `utils-support-common-starter`（Trigger/CronExpression）+ `spring-support-cache-starter`，实现最小可用、运行可视化、可定时调度的爬虫中心。
>
> 重构原则：**质量优先，不赶工**。每阶段完成后跑通验证，再进入下一阶段。

---

## 总体进度

| Day | 阶段 | 状态 | 验证产物 |
|---|---|---|---|
| D1 | utils-support-flow-starter 加 traceId + MDC + FlowSnapshotStore + 节点 schema | ✅ 已完成 | FlowTraceExample 编译+运行通过，快照可读写回 |
| D2 | spring-support-spider-starter 5 张表 + entity + mapper + service + 缓存 + TraceFilter | ✅ 已完成 | 真实启动连 MySQL + curl 全链路验证（CRUD + 缓存 + X-Trace-Id） |
| D3 | spider 节点（request/extract/transform/subLoop/map/output/condition）注册 | ⬜ 待开始 | 每节点单测 |
| D4 | SpiderScheduler（每秒 + CronExpression）+ ExecutionService + 日志落库 | ⬜ 待开始 | 后端启动，cron 触发跑通 |
| D5 | pages/spider/ 4 视图（definitions/designer/data-records/executions）— ReTable + 基础表单（不含 ReFlow） | ⬜ 待开始 | 浏览器真跑 |
| D6 | ReFlow.vue 基础（logicflow 集成 + 拖入 palette + 连线 + 保存 JSON） | ⬜ 待开始 | 浏览器画图存盘 |
| D7 | ReFlow 配置面板（节点 schema 动态表单）+ 暗色主题 | ⬜ 待开始 | 编辑节点 |
| D8 | Spider SSE 实时（后端 SseEmitter + 前端 EventSource）+ 节点呼吸/徽章 | ⬜ 待开始 | 运行中看节点状态实时变 |
| D9 | 全链路 traceId 视图（按 executionNo 串联所有节点日志 + 时间线） | ⬜ 待开始 | 点一次看完整链路 |
| D10 | 收尾打磨 + 真实 demo + 完整跑通截图 + 推送 gitee | ⬜ 待开始 | 截图交付 |

---

## 包结构

```
spring-support-parent-starter/
  └── spring-support-spider-starter/                    # 新建
      ├── pom.xml                                        # 依赖 flow / common / cache / mysql / mybatis-plus
      └── src/main/java/com/chua/starter/spider/
          ├── entity/         SpiderDefinition / SpiderFlow / SpiderDataRecord / SpiderExecutionLog / SpiderEnv
          ├── mapper/         *Mapper.java × 5
          ├── service/        SpiderDefinitionService (@CacheConfig) / SpiderFlowService / ...
          ├── node/           SpiderRequestNode / SpiderExtractNode / SpiderTransformNode / SpiderSubLoopNode / SpiderMapNode / SpiderOutputNode / SpiderConditionNode
          ├── scheduler/      SpiderScheduler
          ├── execution/      SpiderExecutionService
          ├── trace/          SpiderTraceFilter
          ├── sse/            SpiderSseController
          └── config/         SpiderAutoConfiguration
utils-support-core-parent/
  └── utils-support-flow-starter/                        # 改造
      └── 增: FlowContext.traceId / MDC / FlowSnapshotStore / FlowNodeMetadata.configSchema
vue-support-parent-starter/
  ├── packages/ui/src/components/ReFlow/                 # 新建
  │   ├── index.ts
  │   ├── ReFlow.vue                                     # logicflow 集成
  │   ├── NodePalette.vue                                # 左侧节点列表
  │   ├── NodeConfigPanel.vue                            # 右侧配置抽屉（schema 驱动）
  │   └── Toolbar.vue                                    # 保存/清空/缩放
  └── pages/spider/                                      # 新建
      ├── src/router/index.ts                            # 4 路由（definitions/designer/data-records/executions）
      └── src/views/
          ├── DefinitionsView.vue
          ├── DesignerView.vue                            # ReFlow 容器
          ├── DataRecordsView.vue
          └── ExecutionsView.vue
```

---

## D1 — utils-support-flow-starter 加 traceId + MDC + FlowSnapshotStore

**目标**：单次 flow 执行能产出 traceId，贯穿日志，节点前后可序列化持久化。

**改动清单**：
- [x] `DefaultFlowInstance.run()` 入口 `MDC.put("traceId", traceId)`，结束清理
- [x] 新增 `FlowSnapshotStore` 接口（`save(snapshot)` / `load(executionNo)` / `list`）
- [x] 提供默认 `MemoryFlowSnapshotStore` 实现
- [x] `FlowNodeMetadata` 加 `List<FlowNodeField> configSchema`（供前端按字段渲染表单），`FlowNode.configSchema()` 默认空
- [x] `FlowEngine.registerSnapshotStore()` + `snapshotStore()`（默认 MemoryFlowSnapshotStore）

**验证**：
- [x] `FlowTraceExample` 编译+运行通过：流程 JSON 导出、parseJson 执行、status=COMPLETED、snapshotStore.load(executionNo) 取回完整快照
- [ ] 需 `spring-support-flow-starter` Web 场景透过 response header `X-Trace-Id` 验证（D2 后）

---

## D2 — spring-support-spider-starter 工程骨架

**目标**：建包、加依赖、建 5 张表、能 CRUD。

**改动清单**：
- [x] 新建 `spring-support-spider-starter/pom.xml`，依赖 `spring-support-cache-starter` / `utils-support-flow-starter` / mysql / mybatis-plus
- [x] 新建 `SpiderAutoConfiguration` 注册到 `META-INF/spring/...AutoConfiguration.imports`
- [x] 5 张表 DDL（放在 `src/main/resources/db/init/V1.0.0__init_spider.sql`，由 datasource-starter 自动建表）
- [x] 5 个 entity（用 MyBatis-Plus `@TableName`/`@TableId`，继承 `SysBase`）
- [x] 5 个 mapper
- [x] 5 个 service（CRUD + cache 注解）
- [x] 5 个 controller（REST 接口，路径 `/spider-api/spider/*`）
- [x] SpiderTraceFilter（生成 traceId → MDC → response header `X-Trace-Id`）
- [x] 配置 `application.yml` 走 MySQL `172.16.0.40:3306 root/root@`

**`@Cacheable` 缓存策略**：
- `SpiderDefinitionService` 类级 `@CacheConfig(cacheNames = "spider")`
- `listEnabled()` `@Cacheable(key = "'definitions:enabled'")`
- `save/update/deleteById` `@CacheEvict(allEntries = true)`

**验证**：
- 起 SpiderTestApplication（真实启动连接 MySQL spider 库，端口 19091）
- curl 全链路验证通过：
  - `POST /spider-api/spider/definitions` 新增（createTime/updateTime 由 MetaObjectHandler 自动填充）
  - `GET .../definitions/list`、`list-enabled`（enabled 过滤 + @Cacheable 缓存）
  - `GET .../definitions/{id}`、`PUT` 更新、`DELETE` 逻辑删除（sys_deleted=0/1）
  - `POST/GET .../envs`、`GET .../envs/map-enabled`（返回 `{COOKIE: "a=1;b=2"}`）
  - `POST .../flows`、`GET .../flows/by-definition/{id}`（nodes_json/edges_json JSON 存取）
  - 所有响应头含 `X-Trace-Id`
- 自动建表验证：datasource-starter ONCE 模式扫描 `db/init/V1.0.0__init_spider.sql`，5 张表 + `sys_database_version` 落库

**D2 踩坑记录**：
- `application.yml` 用 `spring.sql.init` + 根目录 `schema.sql` 会与 datasource-starter 自动建表冲突 → 已删除 schema.sql，改由 `db/init/*.sql` 负责
- JDBC URL `characterEncoding=utf8mb4` 在 MySQL Connector/J 9.x 非法 → 改 `UTF-8`
- `.m2` 中 common-starter jar 曾残留旧版导致 `RequestUtils` ClassNotFoundException → 重新 install 源码
- 旧 schema.sql 遗留 8 张 spider_* 脏表使 ONCE 模式误判"库非空"跳过建表 → 清库后自动建 5 张新表
- MetaObjectHandler 需由 mybatis-starter 的 `sqlSessionFactory` 注入 `GlobalConfig`（手动构造的 SqlSessionFactory 不会自动关联），且 handler 建议用 `@Bean` 注册而非 `@Component @ConditionalOnMissingBean`

---

## D3 — 爬虫节点实现

**目标**：7 个节点（request / extract / transform / subLoop / map / output / condition）+ 注册到 `FlowNodeRegistry`。

**节点清单**：

| 节点 key | 用途 | 输入 | 输出 | configSchema |
|---|---|---|---|---|
| `spider.request` | HTTP 请求 | url, method, headers, body, timeoutMs | `$.body / status / headers` | url: input, method: select, headers: kv, body: textarea, timeoutMs: number |
| `spider.extract` | 提字段 | source 表达式指向 body/上一层 | 写到 context by saveAs | source:input, parserType:select(jsonpath/regex/css), expression:input, saveAs:input, isList:switch |
| `spider.transform` | 字符串/时间转换 | input | saveAs | field, format(select/none/upper/lower/yMd/yMdHms/...), regex, replacement |
| `spider.subLoop` | 数组循环 | source(arr) | 每条 context | source, as(item) |
| `spider.map` | key/value 映射 | source | saveAs | mappings[{from, to, defaultValue}] |
| `spider.output` | 写 spider_data_record | source 数据 | 写入 | mapping{title, content, ...} |
| `spider.condition` | 条件分支 | field, op, value | 下一节点 | field, op(eq/ne/gt/lt/contains/regex), value |

**改动清单**：
- [ ] 7 个节点类实现 `FlowNode` 接口
- [ ] 在 `SpiderAutoConfiguration` 用 `@PostConstruct` 注册到 `FlowNodeRegistry`
- [ ] 节点配置 `metadata.configSchema`（前端按它生成表单）
- [ ] 环境变量插值：`{{env.COOKIE}}` 在 request/extract 节点 URL/expression 里自动替换（读 `spider_env` 表）

**验证**：
- 每个节点写 JUnit 测试，调一次看上下文 KV 是否正确

---

## D4 — SpiderScheduler + ExecutionService

**目标**：1 秒轮询 + cron 比对 + 触发 flow 执行 + 落库日志。

**改动清单**：
- [ ] `SpiderScheduler` (`@Component`) — `ScheduledExecutorService` 每 1s 调 `tick()`
- [ ] `tick()` 调 `definitionService.listEnabled()`（命中缓存）→ 对每条判定 `CronExpression.getNextValidTimeAfter(lastRun)` → 到点调 `executionService.execute(def)`
- [ ] `SpiderExecutionService.execute(def)` —
  1. 生成 `executionNo = UUID`
  2. 读 `SpiderFlow` 的 `nodes_json/edges_json` 拼成 flow JSON
  3. `FlowEngine.parseJson(json).createGraph().createInstance().run()`（透传 traceId）
  4. 注册 `FlowSnapshotStore` 持久化实现 `SpiderLogSnapshotStore`，每节点 before/after 写 `spider_execution_log`
- [ ] 写后更新 `spider_definition.last_run_at/status/message`
- [ ] 起一个 demo：`code=demo_example` 配 cron `0/30 * * * * ?`、URL `http://example.com`、extract `title`

**验证**：
- 后端启动 → 等 30 秒看 `spider_execution_log` 至少 1 条 SUCCESS
- 看 `spider_data_record` 已写入 `example.com` 的 title

---

## D5 — 前端 4 视图（基础版，无 ReFlow）

**目标**：先用最简方式（ReTable + 表单）跑通，不含可视化编排。

**改动清单**：
- [ ] `packages/ui/src/index.ts` 新增 `@repo/ui/re-flow`（先 stub，下一日做）
- [ ] `pages/spider/` 新建路由 + 4 个 view：
  - `/spider/definitions` — ReTable + 新增/编辑抽屉（表单）
  - `/spider/data-records` — ReTable
  - `/spider/executions` — ReTable + 详情页（traceId 全文）
  - `/spider/designer/:code` — 先放占位（"ReFlow 编排：D6 上线"）
- [ ] 4 视图全部用 koi 主题暗色
- [ ] 调 httpClient 指向 `http://localhost:19091/spider-api`（先不用 proxy）

**验证**：
- 浏览器打开 `/spider/definitions`：可见表 + 新增按钮
- 新增一个 demo → 看列表刷新
- `/spider/executions` 看见 D4 跑出的日志
- `/spider/data-records` 看见 example.com 的 title

---

## D6 — ReFlow.vue 基础（logicflow 集成）

**目标**：能拖节点 + 连线 + 保存 JSON。

**改动清单**：
- [ ] `packages/ui/src/components/ReFlow/ReFlow.vue` — 集成 `@logicflow/core`
- [ ] 组件 props: `nodes`（类型清单）/ `edges`（已有连线）/ `modelValue`（双向 JSON）
- [ ] 内置 7 个 spider 节点类型（圆角矩形 + 不同 header icon/颜色）
- [ ] 左 palette + 中画布 + 顶 toolbar（保存/清空/缩放/适配）
- [ ] 保存事件 emit JSON 落到 `spider_flow.nodes_json/edges_json`
- [ ] 暗色 dot-grid 背景

**验证**：
- `/spider/designer/:code` → 拖 Request + Extract + Output 三节点，连两条线 → 保存
- 后端 `spider_flow.nodes_json/edges_json` 内容正确
- 刷新页面节点位置保留

---

## D7 — ReFlow 配置面板 + 暗色主题

**目标**：点击节点弹出抽屉，按节点 schema 渲染表单。

**改动清单**：
- [ ] `ReFlow` emits `node-click`
- [ ] `NodeConfigPanel.vue` — 抽屉，宽度 380px
- [ ] 按节点 `configSchema` 动态渲染 ReForm：
  - url/method/body → input / select / textarea
  - headers/mappings → key-value 编辑器
- [ ] 环境变量插值提示：编辑 URL/expression 时显示已有的 `{{env.*}}` token，高亮浅蓝
- [ ] 全部按上面设计系统走（间距/圆角/字号/动效）

**验证**：
- 编辑 Request 节点 → URL/headers 字段 OK
- 保存回到 `spider_flow`

---

## D8 — SSE 实时（节点呼吸 + 徽章）

**目标**：运行中节点实时变色、徽章、动画。

**改动清单**：
- [ ] 后端 `SpiderSseController` — `@GetMapping("/spider/sse/execution/{executionNo}")` 返回 `SseEmitter`
- [ ] `SpiderExecutionService` 节点执行完 push 事件 `{nodeKey, status, ts}`
- [ ] 前端 `pages/spider/designer/:code` 加 EventSource 订阅
- [ ] 节点状态：
  - pending → 不渲染状态
  - running → cyan border + breathing animation（1.5s linear infinite alternate，opacity 1↔0.4，box-shadow cyan/30）
  - success → emerald border + ✓ badge
  - fail → red border + ✗ badge
- [ ] CSS class：`node-running / node-success / node-fail` + `@keyframes breathing`

**验证**：
- 跑 demo，浏览器看节点依次变 running → success，呼吸效果可见

---

## D9 — 全链路 traceId 视图

**目标**：一次执行能整链路查看所有节点日志 + 时间线。

**改动清单**：
- [ ] `ExecutionsView.vue` 加详情抽屉/页：
  - 顶部 traceId（大字）+ 复制按钮
  - 中间时间线（每节点一个 row，水平时间轴显示 start/end）
  - 每个节点 row 点击展开看 input JSON / output JSON / error
- [ ] 后端 `GET /spider-api/spider/executions/logs?executionNo=xxx` 返回整链路
- [ ] 时间线组件用 CSS grid 实现，不引第三方

**验证**：
- 跑一次 → 进入详情 → 看到 traceId 一致、所有节点按执行顺序

---

## D10 — 收尾 + 推送 gitee

**改动清单**：
- [ ] 跑一个完整 demo：http://example.com，全链路截图
- [ ] 跑一个 cron demo：5 秒一次，截图 5 个节点都看到
- [ ] 真实跑 1 分钟浏览器视频（5 张以上截图）
- [ ] 三仓库推送：
  - `utils-support-parent-starter` master → origin
  - `spring-support-parent-starter` master → origin
  - `vue-support-parent-starter` newmaster → origin
- [ ] 更新根 `README.md` 加新模块说明

**验证交付**：
- 所有 sprint 截图保存到 `D:\ch\project\spider-shots\final\`
- git push 三个仓库无冲突
- 用户在 http://localhost:19092 真能完整体验

---

## 风险与回退

| 风险 | 缓解 |
|---|---|
| logicflow 与 Vue 3.5 + Vite 8 兼容 | 装最新稳定版，集成失败回退 D5 先占位 |
| SpiderScheduler 1 秒扫可能压力 | `@Cacheable` 全表缓存命中，写时 `allEntries=true` 清；评估后压测 |
| SSE 在反向代理后超时 | Spring 默认 30min 心跳，加 HeartbeatTask |
| MyBatis-Plus JSON 字段映射 | 用 fastjson2 JacksonHandler + @TableField(typeHandler) |
| traceId 与现有 md 体系冲突 | flow 包 trace 字段单独命名 `flowTraceId` |

---

## 引用资源

- 设计系统：`./架构图.md`
- 实时进度：按本文件勾选状态
- 变更记录：`./CHANGELOG.md`
