[package]
name = "rust_filesearch"
version = "0.1.0"
edition = "2021"
authors = ["CH"]

[lib]
name = "file_search"
crate-type = ["cdylib"]

[dependencies]
walkdir = "2.5"
serde_json = "1.0"

[profile.release]
opt-level = 3
lto = true
strip = true